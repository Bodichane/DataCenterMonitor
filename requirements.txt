# Versions compatibles Python 3.12+ (les versions précédentes, flasgger 0.9.5 en
# particulier, importaient le module `imp` retiré depuis Python 3.12).
flask>=3.0,<4.0
flasgger>=0.9.7.1
flask-cors>=4.0
gunicorn>=21.2
